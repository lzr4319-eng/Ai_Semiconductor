# 系统性能优化建议报告

## 执行摘要

经过全面代码审查，发现以下主要性能瓶颈和优化机会：

1. **缺少数据缓存机制** - 导致重复读取CSV和JSON文件
2. **频繁的进程检查** - 使用多个subprocess调用检查进程状态
3. **重复计算** - KPI数据在多个地方重复计算
4. **同步任务执行** - 阻塞UI响应
5. **低效的文件I/O** - 多次打开/关闭文件
6. **图片路径查找** - 每次渲染都搜索多个候选路径
7. **HTML处理开销** - 大量正则表达式处理

---

## 详细优化建议

### 1. 添加Streamlit缓存机制 ⭐⭐⭐⭐⭐

**问题**：每次页面刷新都重新读取CSV文件，没有缓存机制。

**影响**：高 - 数据加载是核心操作，频繁重复读取严重影响性能

**优化方案**：

#### 1.1 在 `utils_config.py` 中添加数据加载缓存

```python
@st.cache_data(ttl=3600, show_spinner=False)  # 缓存1小时
def load_data_cached(file_path: str):
    """缓存数据加载"""
    return pd.read_csv(file_path)

@st.cache_data(ttl=3600)
def load_json_cached(file_path: str):
    """缓存JSON加载"""
    if os.path.exists(file_path):
        with open(file_path, 'r', encoding='utf-8') as f:
            return json.load(f)
    return None
```

#### 1.2 在 `descriptive_stats.py` 中缓存KPI计算

```python
@st.cache_data(ttl=300)  # 缓存5分钟
def load_kpi_data_cached(base_dir: str):
    """缓存KPI数据计算"""
    # ... 现有逻辑 ...
    return kpi_data
```

**预期收益**：
- 数据加载速度提升 **80-90%**
- 减少磁盘I/O操作 **90%+**

---

### 2. 优化进程检查逻辑 ⭐⭐⭐⭐

**问题**：`descriptive_stats.py` 中 `_is_pid_running()` 函数使用多个subprocess调用检查进程状态。

**影响**：中高 - 每3秒检查一次，产生大量系统调用开销

**当前代码问题**：
```python
# 问题：多次subprocess调用
res = subprocess.run(["ps", "-p", str(pid), "-o", "stat=,pid="], ...)
pgrep_res = subprocess.run(["pgrep", "-P", str(pid)], ...)
python_processes = subprocess.run(["pgrep", "-f", "..."], ...)
```

**优化方案**：

```python
import psutil  # 需要添加到requirements.txt

def _is_pid_running(pid: int) -> bool:
    """使用psutil库高效检查进程状态"""
    if not pid:
        return False
    try:
        process = psutil.Process(pid)
        # 检查进程是否还在运行
        if not process.is_running():
            return False
        
        # 检查是否有子进程
        children = process.children(recursive=True)
        if children:
            return True
        
        # 检查相关Python进程（仅检查一次）
        for proc in psutil.process_iter(['pid', 'name', 'cmdline']):
            try:
                cmdline = proc.info.get('cmdline', [])
                if any('eda.py' in str(cmd) or 'ml.py' in str(cmd) or 
                       'posion.py' in str(cmd) or 'ai_text_analyzer.py' in str(cmd) 
                       for cmd in cmdline):
                    return True
            except (psutil.NoSuchProcess, psutil.AccessDenied):
                continue
        return True
    except psutil.NoSuchProcess:
        return False
    except Exception:
        return False
```

**预期收益**：
- 进程检查速度提升 **70-80%**
- 减少系统调用 **60%+**

---

### 3. 优化文件I/O操作 ⭐⭐⭐⭐

**问题**：多次打开/关闭同一文件，特别是在日志读取和图片路径查找中。

**优化方案**：

#### 3.1 缓存日志读取

```python
@st.cache_data(ttl=2)  # 缓存2秒（日志文件变化频繁）
def _tail_log_cached(log_path: str, n: int = 40) -> str:
    """缓存日志尾部读取"""
    if not log_path or not os.path.exists(log_path):
        return ""
    try:
        with open(log_path, "r", encoding="utf-8", errors="ignore") as f:
            lines = f.readlines()
        return "".join(lines[-n:])
    except Exception:
        return ""
```

#### 3.2 缓存图片路径查找结果

```python
# 在模块级别添加缓存字典
_image_path_cache = {}

def replace_image_paths(html_content):
    """优化图片路径替换，使用缓存"""
    pattern = r'src=["\']([^"\']+)["\']'
    missing = []
    
    def replace_func(match):
        img_path = match.group(1)
        if img_path.startswith('data:image'):
            return match.group(0)
        
        # 检查缓存
        cache_key = img_path
        if cache_key in _image_path_cache:
            cached_path = _image_path_cache[cache_key]
            if cached_path and os.path.exists(cached_path):
                b64 = get_base64_image(cached_path)
                if b64:
                    return f'src="{b64}"'
        
        # ... 现有查找逻辑 ...
        
        # 缓存结果
        _image_path_cache[cache_key] = full_path
        
        # ... 返回结果 ...
    
    return re.sub(pattern, replace_func, html_content), missing
```

**预期收益**：
- 文件I/O操作减少 **50-70%**
- 图片路径查找速度提升 **60-80%**

---

### 4. 优化任务状态检查频率 ⭐⭐⭐

**问题**：每3秒自动刷新，即使任务状态未变化。

**优化方案**：

```python
# 在 descriptive_stats.py 中优化刷新逻辑
if running and job:
    current_time = time.time()
    last_check = st.session_state.get('last_task_check', 0)
    last_status = st.session_state.get('last_job_status_hash', '')
    
    # 检查任务状态是否真的变化了
    current_status_hash = f"{pid_running}_{task_completed}_{task_failed}"
    
    # 只有当状态变化或超过3秒才刷新
    if current_time - last_check > 3 and current_status_hash != last_status:
        st.session_state['last_task_check'] = current_time
        st.session_state['last_job_status_hash'] = current_status_hash
        st.rerun()
```

**预期收益**：
- 减少不必要的页面刷新 **40-60%**
- 降低CPU使用率 **30-50%**

---

### 5. 优化数据计算复用 ⭐⭐⭐⭐

**问题**：KPI数据在多个地方重复计算（`descriptive_stats.py`, `ai_text_analyzer.py`, `html_generate.py`）

**优化方案**：

#### 5.1 创建共享的数据计算模块

创建新文件 `base_function/main/data_cache.py`：

```python
import pandas as pd
import os
from functools import lru_cache
from typing import Dict, Optional

_data_cache = {}
_cache_timestamps = {}

def get_kpi_data(base_dir: str, force_refresh: bool = False) -> Dict:
    """获取KPI数据，带缓存"""
    data_file = os.path.join(base_dir, 'base_function', 'output', 'cleaned_chip_data_final.csv')
    cache_key = f"kpi_{data_file}"
    
    # 检查文件修改时间
    if os.path.exists(data_file):
        file_mtime = os.path.getmtime(data_file)
        cached_mtime = _cache_timestamps.get(cache_key, 0)
        
        # 如果文件未修改且缓存存在，直接返回
        if not force_refresh and cache_key in _data_cache and file_mtime == cached_mtime:
            return _data_cache[cache_key]
        
        # 重新计算
        df = pd.read_csv(data_file)
        kpi_data = _calculate_kpi(df)
        
        # 更新缓存
        _data_cache[cache_key] = kpi_data
        _cache_timestamps[cache_key] = file_mtime
        
        return kpi_data
    
    return {}

def _calculate_kpi(df: pd.DataFrame) -> Dict:
    """统一的KPI计算逻辑"""
    # ... 将现有计算逻辑移到这里 ...
    pass
```

#### 5.2 在所有模块中使用共享函数

```python
# 在 descriptive_stats.py, ai_text_analyzer.py 等文件中
from data_cache import get_kpi_data

kpi_data = get_kpi_data(base_dir)
```

**预期收益**：
- 减少重复计算 **100%**（同一数据只计算一次）
- KPI计算时间减少 **70-80%**

---

### 6. 优化HTML处理 ⭐⭐⭐

**问题**：`ai_text_analyzer.py` 中使用大量正则表达式处理HTML，每次生成报告都执行。

**优化方案**：

#### 6.1 预编译正则表达式

```python
# 在模块级别预编译所有正则表达式
_HTML_PATTERNS = {
    'code_block_start': re.compile(r'^```html?\s*\n?', re.MULTILINE),
    'code_block_end': re.compile(r'\n?```\s*$', re.MULTILINE),
    'img_src': re.compile(r'src=["\']([^"\']+)["\']'),
    'nested_div': re.compile(r'<div class="section-card">\s*<div class="section-card">'),
    # ... 其他模式 ...
}

def clean_html_content(content: str) -> str:
    """使用预编译的正则表达式清理HTML"""
    # 使用预编译的模式
    content = _HTML_PATTERNS['code_block_start'].sub('', content)
    content = _HTML_PATTERNS['code_block_end'].sub('', content)
    # ... 其他处理 ...
    return content
```

**预期收益**：
- HTML处理速度提升 **30-40%**
- 减少正则表达式编译开销

---

### 7. 优化图片Base64编码 ⭐⭐⭐

**问题**：每次渲染都重新编码图片为Base64。

**优化方案**：

```python
# 添加Base64编码缓存
_base64_cache = {}
_base64_cache_timestamps = {}

def get_base64_image(image_path: str) -> Optional[str]:
    """带缓存的Base64编码"""
    if not os.path.exists(image_path):
        return None
    
    # 检查缓存
    file_mtime = os.path.getmtime(image_path)
    cache_key = image_path
    
    if cache_key in _base64_cache:
        cached_mtime = _base64_cache_timestamps.get(cache_key, 0)
        if file_mtime == cached_mtime:
            return _base64_cache[cache_key]
    
    # 重新编码
    try:
        with open(image_path, "rb") as f:
            encoded = base64.b64encode(f.read()).decode('utf-8')
            ext = os.path.splitext(image_path)[1].lower()
            mime = "image/png" if ext == ".png" else "image/jpeg"
            result = f"data:{mime};base64,{encoded}"
        
        # 更新缓存
        _base64_cache[cache_key] = result
        _base64_cache_timestamps[cache_key] = file_mtime
        
        return result
    except:
        return None
```

**预期收益**：
- 图片编码时间减少 **90%+**（缓存命中时）
- 减少内存中的重复数据

---

### 8. 优化子进程管理 ⭐⭐⭐

**问题**：使用bash包装脚本执行任务，增加了进程管理复杂度。

**优化方案**：

考虑使用Python的 `multiprocessing` 或 `concurrent.futures` 替代bash脚本：

```python
from concurrent.futures import ProcessPoolExecutor, as_completed
import sys

def _run_text_pipeline_async():
    """使用进程池异步执行任务"""
    job_id = uuid.uuid4().hex[:8]
    timestamp = time.strftime('%Y%m%d_%H%M%S')
    log_path = f"/tmp/lzr_text_{timestamp}_{job_id}.log"
    
    st.session_state.job = {
        "id": job_id,
        "name": "text",
        "pid": None,
        "start_ts": time.time(),
        "log_path": log_path,
    }
    
    # 使用进程池执行
    with ProcessPoolExecutor(max_workers=1) as executor:
        future = executor.submit(_execute_pipeline_steps, eda_script, position_script, text_script, log_path)
        st.session_state.job['future'] = future
    
    st.rerun()

def _execute_pipeline_steps(eda_script, position_script, text_script, log_path):
    """在子进程中执行所有步骤"""
    steps = [
        ("EDA分析", eda_script),
        ("位置分析", position_script),
        ("AI分析", text_script),
    ]
    
    for step_name, script_path in steps:
        with open(log_path, "a", encoding="utf-8") as lf:
            subprocess.run(
                [sys.executable, script_path],
                cwd=os.path.dirname(script_path),
                stdout=lf,
                stderr=subprocess.STDOUT,
                check=True,
                text=True,
            )
```

**预期收益**：
- 进程管理更简单、更可靠
- 减少bash脚本的维护成本

---

### 9. 添加数据预加载 ⭐⭐

**问题**：用户首次访问时需要等待数据加载。

**优化方案**：

在应用启动时预加载常用数据：

```python
# 在 app.py 中添加
@st.cache_resource
def preload_common_data():
    """预加载常用数据"""
    base_dir = os.path.dirname(os.path.dirname(os.path.dirname(__file__)))
    data_file = os.path.join(base_dir, 'base_function', 'output', 'cleaned_chip_data_final.csv')
    
    if os.path.exists(data_file):
        return pd.read_csv(data_file)
    return None

# 在页面加载时调用
if 'preloaded_data' not in st.session_state:
    with st.spinner("正在预加载数据..."):
        st.session_state.preloaded_data = preload_common_data()
```

---

### 10. 优化requirements.txt ⭐⭐

**建议添加**：
```txt
psutil>=5.9.0  # 高效的进程管理库
```

---

## 优先级排序

1. **高优先级（立即实施）**：
   - ✅ 添加Streamlit缓存机制（#1）
   - ✅ 优化进程检查逻辑（#2）
   - ✅ 优化数据计算复用（#5）

2. **中优先级（近期实施）**：
   - ✅ 优化文件I/O操作（#3）
   - ✅ 优化任务状态检查频率（#4）
   - ✅ 优化HTML处理（#6）

3. **低优先级（可选）**：
   - ✅ 优化图片Base64编码（#7）
   - ✅ 优化子进程管理（#8）
   - ✅ 添加数据预加载（#9）

---

## 预期总体收益

实施所有高优先级优化后：
- **页面加载速度提升**: 60-80%
- **CPU使用率降低**: 40-60%
- **内存使用优化**: 20-30%
- **用户体验改善**: 显著减少等待时间

---

## 实施建议

1. **分阶段实施**：先实施高优先级优化，测试稳定后再进行中低优先级优化
2. **性能测试**：每次优化后使用 `time.time()` 或 `cProfile` 测量性能改善
3. **监控**：添加性能监控日志，跟踪关键操作的执行时间
4. **回滚计划**：保留原始代码备份，确保可以快速回滚

---

## 注意事项

1. **缓存失效**：确保缓存机制在数据更新时能正确失效
2. **内存管理**：大量缓存可能增加内存使用，需要监控
3. **并发安全**：多用户访问时确保缓存机制线程安全（Streamlit的`@st.cache_data`已处理）
4. **依赖管理**：添加新依赖（如`psutil`）需要更新`requirements.txt`
